## תרגיל 1

1. זה ענף(מצביע לכומיט האחרון) שבו אפשר לעבוד בנפרד ממה שעובד עכשיו בפרודקשן ורק אחרי שיעבור את כל הבדיקות והישורים יהיה אפשר למזג אותו עם הקוד הקיים, כדי לא להכניס לשם באגים
2. במאין נמצא הקוד המרכזי שועובד לעוצת הענף פיצר בראנץ ששם מפתחים פחצר מסויים
3. ממש לא זה טירוף...

## תרגיל 2

1. git checkout -b fix/login-error
2. git switch fix/login-error
3. git switch main

## תרגיל 3

1. הד זה איפה שאני נמצא עכשיו וזה באיזה ענף שאני עובד
2. ההד יצביע על מאין
3. לא נכון הוא דינמי אחרת המערכת של גיט לא תדע איפה אני נמצא ולאיפה לשייך כומיטים

## תרגיל 4

1. git switch main
2. git switch main git merge feature/signup-form
3. כי לא צריך אותו וזה יוצר בלאגן

## תרגיל 5

1. git checkout -b feature/notifications
2. git commit -m "Add notification bell icon"
3. git branch
4. git checkout main
5. git checkout main / git merge feature/notifications / git branch -d feature/notifications

## שאלות סיכום

1. כי כל אחד עבד על ענף נפרד
2. כופליכטים ובאגים
3. תיקונים זעירים
